import { StyleSheet } from "react-native";

export const dropdownStyles = StyleSheet.create({
    container: {
        width: 85,
        paddingHorizontal: 4
    },
    dropdown: {
        height: 50,
        borderColor: '#dcdcdc',
        borderWidth: 0.5,
        borderRadius: 8,
        paddingHorizontal: 8,
        color: "#000"
    },
    placeholderStyle: {
        fontSize: 14,
        color: "#777"
    },
    iconStyle: {
        width: 24,
        height: 24,
    },
});